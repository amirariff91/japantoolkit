Major - [lib/site.ts:44](/home/node/.openclaw/workspace/japantoolkit/lib/site.ts#L44) / [app/sitemap.ts:37](/home/node/.openclaw/workspace/japantoolkit/app/sitemap.ts#L37): `ic-card-japan` was added as a published guide and is linked from the guides hub, itinerary pages, and other guides, but it is missing from the shared `guides` array that drives `sitemap.xml`. As shipped, `/guides/ic-card-japan` has a canonical URL and internal links but is silently excluded from the sitemap, so one of today’s new guide pages is not being submitted to search engines.

Major - [app/api/subscribe/route.ts:202](/home/node/.openclaw/workspace/japantoolkit/app/api/subscribe/route.ts#L202): the route awaits `resend.emails.send(...)` and then unconditionally returns `{ success: true }`, but it never inspects the SDK response for an `error`. Resend returns provider-level failures in-band, so a rejected send can still produce a 200 response here. That means the UI will tell users the email/checklist was sent when the welcome email was actually not accepted for delivery.

Major - [components/email-capture-form.tsx:43](/home/node/.openclaw/workspace/japantoolkit/components/email-capture-form.tsx#L43) / [components/rail-pass-calculator.tsx:228](/home/node/.openclaw/workspace/japantoolkit/components/rail-pass-calculator.tsx#L228) / [app/api/subscribe/route.ts:116](/home/node/.openclaw/workspace/japantoolkit/app/api/subscribe/route.ts#L116): the frontend still promises a checklist/PDF/calculation copy ("Check your inbox — the checklist is on its way", "Get a copy of your calculation plus our Japan planning checklist"), but today’s subscribe route now sends only the Week 1 onboarding email with a budget-estimator CTA and no checklist asset or saved calculation. This is a user-visible behavior regression on every subscription entry point.

Minor - [app/api/subscribe/route.ts:32](/home/node/.openclaw/workspace/japantoolkit/app/api/subscribe/route.ts#L32) / [app/api/subscribe/route.ts:202](/home/node/.openclaw/workspace/japantoolkit/app/api/subscribe/route.ts#L202): duplicate contacts are treated as success on the audience insert, but the code still sends the Week 1 email afterward. Any repeat submission from the homepage or rail-pass form will resend the onboarding email to existing subscribers, which is likely to create avoidable duplicate sends and user confusion.
